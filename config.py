# Vision-capable Gemini model. Swap this one line to change models app-wide.
# Alternatives: "gemini-2.5-flash-lite" (cheapest, still multimodal — good for a
# demo/personal project), "gemini-3.1-pro" (highest quality, more expensive).
MODEL_ID = "gemini-3.5-flash"

MAX_TOKENS = 2048  # enough for a full differential-diagnosis-format response
